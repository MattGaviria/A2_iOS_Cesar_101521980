import SwiftUI

struct ContentView: View {
    var body: some View {
        NavigationStack {
            Text("Product Catalog")
                .font(.title)
                .padding()
        }
    }
}

#Preview {
    ContentView()
}
